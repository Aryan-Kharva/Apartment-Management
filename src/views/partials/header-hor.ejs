<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="/js/index.js" defer></script>
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <title><%= title %></title>
</head>
<body class="relative">

    <div class="header flex flex-col items-center z-10">
        <div class="w-full flex justify-center items-center mb-4">
            <a href="/"><img src="/images/potato_logo.png" alt="Potato Properties Logo" style="width: 400px;"></a>
        </div>
        <nav class="w-5/6 flex justify-center gap-4">
            <a href="/">Home</a>
            <a href="/properties">Floor Plans</a>
            <a href="/users/contact">Contact</a>
            <%if(!user){%>
            <a href='/users/new'>Join Community</a>
            <a href='/users/login'>Login</a>
            <%} else {%>
            <a href='/users/dashboard'>My Account</a>
            <a href='/users/logout'>Logout</a>
            <%}%>
        </nav>
    </div>
    <% if(errorMessages.length > 0) {%>
        <% errorMessages.forEach(message=>{%>
            <div id="message-container" class="<%= message.category %> flex justify-center fixed top-0 right-0 z-20 p-3 bg-red-200 mt-[1em] mr-[1em] rounded-xl w-1/4 font-bold">
                <h1 class="flex-1 text-center"><%=message%></h1>
            </div>
        <%});%>
    <%}%>
    <% if(successMessages.length > 0) {%>
        <% successMessages.forEach(message=>{%>
            <div id="message-container" class="<%= message.category %> flex justify-center fixed top-0 right-0 z-20 p-3 bg-green-200 mt-[1em] mr-[1em] rounded-xl w-1/4 font-bold">
                <h1 class="flex-1 text-center"><%=message%></h1>
            </div>
        <%});%>
    <%}%>

    